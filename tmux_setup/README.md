### This assumes you have tmux installed already (> v1.9)

- Run the bash script to copy the tmux config to your home dir
- Clone tpm (tmux plugin manager) into aa .tmux/plugins folder
- Open tmux and press Ctrl-I to install the other plugins
